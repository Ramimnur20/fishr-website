{
  "name": "fishr-web",
  "version": "1.0.0",
  "description": "FishR Discord Bot Website with Vercel Serverless API + Redis",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site - no build needed'"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
